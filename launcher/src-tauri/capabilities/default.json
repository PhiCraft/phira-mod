{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "开服器需要的基础权限：事件订阅（实时日志 / 连接通知 / DDNS 通知）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
