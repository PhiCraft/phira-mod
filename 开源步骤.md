# 开源与发布步骤

本文档说明**怎么把这份源码发布出去**，以及发布前必须处理的三个授权问题。
面向的是仓库维护者，不是玩家手玩家看 `launcher/联机使用教程.txt`）。

## 0. 先明确这份代码是什么

- 本项目是 **Phira手`TeamFlos/phira`，0.8.2）的修改版**，引擎部分手`prpr`）与客户端
  手`phira`）都基于上游代码，另加了一批 mod手判定窗口、打击表现、成绩历史、每日挑战等）。
- 上游的许可证是 **GPL-3.0-only**手见根 `Cargo.toml` 的 `[workspace.package] license`）。
  **改版必须继续用 GPL-3.0-only 发布**，并且要保留上游版权声明。
- 还有两个独立的子项目手各自有自己的 workspace）：
  - `duel/` —— 双人对战的协议与服务端手Apache-2.0 是自己写的；注意**其中参考过
    `TeamFlos/phira-mp`手Apache-2.0）的设计**，若要严格可以加一句致谢）
  - `launcher/` —— Windows 开服器手Tauri，自己写的）

## 1. 发布前必须处理的三个授权问题手**这些不处理就别公开**）

### 1.1 `assets/` 里的资源来自官方安装包

`assets/` 下的图片、音效、字体手`font.ttf`、`background.jpg` 等）是从**官方 APK/客户端**
里取出来的，版权属于 Phira 官方。CI 里就是**构建时临时下载官方 APK 再提取**
手见 `.github/workflows/android.yml` 的 `Prepare runtime assets` 步骤），
这正是"不要把它们提交进仓库"的信号。

**做法**：源码仓库里**不要提交这些资源**，让使用者自己从官方包提取手CI 已经这么做了）。
本目录已提供 `Phira-0.8.2-mod-src-nofonts.zip`，就是把这一类文件排除后的版本。

### 1.2 字体：已经从 PingFang 换成自带的开源字体手**已处理**）

原来打包了两个苹果字体：

- `assets/pingfang.otf` —— PingFang SC手苹果字体），界面/菜单在用
- `launcher/ui/fonts/PingFang-HK-Regular.otf` —— PingFang HK手苹果字体），开服器在用

**苹果字体不允许随应用再分发**，所以这两个都已删除，统一改用仓库自带的
`assets/font.ttf`手**Source Han Sans + Saira + Noto** 的整合字体，均为 OFL 系）：
界面字体加载处改成了复用它手`phira/src/lib.rs`，两份 painter 共用同一个 `FontArc`，
不会把 13MB 字体在内存里存两遍），开服器的 `@font-face` 也指向
`launcher/ui/fonts/app-font.ttf`手就是这份 font.ttf 的副本）。

也就是说：**字体这一项现在是干净的**，不需要你再改。如果你想换成别的字体
手比如纯 Noto Sans SC 或思源黑体单字体），只要替换 `assets/font.ttf`
并把 `launcher/ui/fonts/app-font.ttf` 一起换掉即可，代码不用动。

> 注意：`assets/phigros.ttf`手19 KB，打歌界面的分数/连击字形）是从**官方客户端**里
> 提取的小子集字体，严格说仍有版权归属。要彻底干净就把它一起排除，
> 代价是打歌界面的数字会换成 `font.ttf` 的字形。


### 1.3 Android 签名口令是明文

`android/app/build.gradle` 里 `storePassword "phiramod"` / `keyPassword "phiramod"` 是明文。
**keystore 文件本身没有被提交**手已在 `.gitignore` 里），所以现在不算泄密；
但开源后建议改成从环境变量/GitHub Secrets 读，否则别人会以为可以直接用。

## 2. 打包源码手本地就有现成脚本）

`git archive` 只会打包**被 git 跟踪的文件**，天然排除 `target/`、
`launcher.json`手里面有 dynv6 token）、`launcher.log`、keystore 等，是最稳的打包方式：

```bash
cd phira-0.8.2

# 完整源码手含全部资源，38.8 MB）
git archive --format=zip -o ../release/Phira-0.8.2-mod-src.zip HEAD

# 不含受限字体的版本手6.0 MB，适合直接公开托管）
git archive --format=zip -o ../release/Phira-0.8.2-mod-src-nofonts.zip HEAD -- . \
  ':(exclude)assets/font.ttf' \
  ':(exclude)assets/pingfang.otf' \
  ':(exclude)assets/harmonyos.ttf' \
  ':(exclude)launcher/ui/fonts/PingFang-HK-Regular.otf'

# tar.gz手给 Linux/macOS 使用者）
git archive --format=tar.gz -o ../release/Phira-0.8.2-mod-src.tar.gz HEAD
```

**打包后一定要校验**：用压缩软件打开看一眼，确认**没有** `target/`、
没有 `launcher.json` / `launcher.log`、没有 `*.jks` / `*.keystore`、没有 `.env`。

```powershell
# PowerShell 一键校验
Add-Type -AssemblyName System.IO.Compression.FileSystem
$zip = [System.IO.Compression.ZipFile]::OpenRead("Phira-0.8.2-mod-src.zip")
$zip.Entries | Where-Object { $_.FullName -match 'target/|launcher\.json|\.jks|\.env' }
$zip.Dispose()
```

## 3. 开源前检查清单

- [ ] **敏感信息**：`git grep -n -i -E 'dynv6|api[_-]?key|secret|password|bearer'`
      —— 确认命中的都是代码/文档，没有真实 token
- [ ] **本地文件**：`git status --ignored` 看一眼，确认 `launcher.json`、
      `launcher.log`、`background.jpg`、`local.properties`、keystore 都**没被跟踪**
- [ ] **大文件**：`git ls-files` 里 >1MB 的都过一遍手当前有 4 个字体 + 2 个图标 + 1 个音效）
- [ ] **授权**：第 1 节的三个问题都处理了
- [ ] **LICENSE**：保留上游的 `LICENSE`手GPL-3.0），并在 README 里注明
      "基于 TeamFlos/phira 0.8.2 修改"
- [ ] **README**：写清这是什么、怎么编译、怎么装、有哪些 mod、已知问题
- [ ] **编译验证**：干净目录解压后能编过手见第 4 节）

## 4. 验证"解压后能编译"

```bash
# 解压到一个干净目录
unzip Phira-0.8.2-mod-src.zip -d /tmp/phira-check && cd /tmp/phira-check/phira-0.8.2

# 客户端手Windows/Linux/macOS 桌面）
cargo build -p phira-main --no-default-features      # 不编视频，避免依赖 FFmpeg

# Android 目标手需要 NDK）
cargo check -p phira --no-default-features --target aarch64-linux-android

# 开服器手Windows，需要 WebView2）
cd launcher/src-tauri && cargo build --release

# 对战服务端手独立 crate，自带测试）
cd duel && cargo test
```

注意几个坑：
- **`phira-main` 默认 feature 带 `video`，需要 FFmpeg 静态库**手`avformat`）。
  没有就加 `--no-default-features`，代价是不能放谱面背景视频。
- **运行时**需要 `assets/` 且必须有 `font.ttf` 与 `background.jpg`，
  缺了会启动即退出手CI 里用 `abstract.jpg` 顶替 `background.jpg`）。
- 本仓库的 `rust-toolchain` 钉了 nightly；**本地用 `cargo +stable` 更省事**
  手stable 装了 android/ios 目标）。

## 5. 发布到 GitHub

1. 新建仓库手**不要**勾选自动生成 README/LICENSE，避免和现有文件冲突）
2. 关联并推送：
   ```bash
   cd phira-0.8.2
   git remote add origin https://github.com/<你>/<仓库>.git
   git push -u origin main
   ```
   ⚠️ 推之前先做第 3 节检查；如果历史里已经有不该有的文件手比如已经提交过的大资源），
   用 `git filter-repo` 清理，或干脆**用源码包重新初始化一个仓库**手历史更干净）：
   ```bash
   unzip Phira-0.8.2-mod-src-nofonts.zip -d new-repo && cd new-repo/phira-0.8.2
   git init && git add -A && git commit -m "import: Phira 0.8.2 mod"
   ```
3. 写 README：项目简介、**上游出处 + GPL-3.0 声明**、编译步骤、资源从哪来、
   已实现的 mod 清单、已知问题、致谢手TeamFlos/phira、TeamFlos/phira-mp）
4. 打开 CI：`.github/workflows/android.yml` 与 `ios.yml` 都是 `workflow_dispatch`
   手手动触发）。iOS 那条在 macOS runner 上跑手10 倍计费），Android 那条 1 倍。
   两个 workflow 都会在构建时自动补齐缺失资源手从官方 APK 提取）。
5. 打 tag 发 Release：iOS workflow 在 `push: tags: v*` 时会自动跑并上传
   `Phira-<版本>-ios<最低版本>-mod-unsigned.ipa` 作为 artifact。
   **产物是无签名的 IPA**，使用者要用 AltStore/Sideloadly 自签。

## 6. 发布前最后一遍自检手照着念一遍）

1. 全新克隆 → `cargo check -p phira --no-default-features` 能过？
2. `assets/` 里没有官方专属资源？两个 PingFang 字体处理了？
3. `launcher.json` / `launcher.log` / keystore / `.env` 都没跟踪？
4. README 里写清了「基于 TeamFlos/phira 修改」和 GPL-3.0？
5. 玩家向文档手`launcher/联机使用教程.txt`）里的示例 IP、域名是不是你自己家的？
   —— 里面现在有一处示例写的是真实公网 IPv6，**发布前记得替换成示例值**。
