# 新会话提示词（复制下面整段发给新对话）

```text
继续 Phira 改版项目里的「逆向官方成绩上传」这件事（纯静态逆向，控制流平坦化 + 手工 GOT）。

【先读这两份，不要重新调查】
1. E:\Code\Rust\Phigros\phira-0.8.2\docs\上传成绩逆向笔记.md   —— 14 节完整调查记录
2. E:\Code\Rust\Phigros\phira-0.8.2\docs\rev\                 —— 逆向脚本 + ptr_map.json
   （deobf.py / chain.py / table.py / annotate.py / find_*.py / ptr_map.txt|json）

【环境事实（都实测过，别踩坑）】
- 工作区 E:\Code\Rust\Phigros；开发仓库 phira-0.8.2（远端 PhiCraft/phigros，git 会自动改写走 ghfast 代理）；
  发布仓库 publish\phira-mod\src（远端 PhiCraft/phira-mod）。两者都要同步提交推送。
- Rust 一律用 cargo +stable（nightly 在这台机器上半装，会卡住）。
- 反汇编用 NDK 自带的：E:\tools\android-sdk\ndk\28.2.13676358\toolchains\llvm\prebuilt\windows-x86_64\bin\llvm-objdump.exe
  （mingw 的 objdump 不认 aarch64；rustup 的 llvm-tools 在这台机器下载会超时；本机没有 pip）
- 源码文件是 CRLF：PowerShell 精确替换要用 "`r`n"，否则匹配不到。
  PowerShell 的 uint64 相加会溢出成 double —— 所有 mod 2^64 的算术都用 Python 写，并设 PYTHONIOENCODING=utf-8。
- 逆向素材在 E:\Code\Rust\Phigros\rev\（未进 git）：
  libphira.so（官方 arm64，未 strip）、official.apk、
  phira-pc\phira-main.exe（官方 PC 客户端）、
  uploads.txt（抓到的真实上传：chart=46304，分数 9550 / 109839，token 133 字节）、
  capture_addon.py + 开始抓包.ps1（抓包环境；mitmdump 实际路径 C:\Program Files\mitmproxy\bin）

【已确认的结论，直接用，别重复验证】
- 编码器符号：encode_record @ 0xd24860 (6328B)、custom_enc @ 0xd24104 (1884B)、
  custom_dec @ 0x929c48、resolve_data @ 0x92a7d8（均为导出符号）
- 上传接口：POST /play/upload，体 {chart, token, chartUpdated}，token = base64(133 字节)
- 混淆规则：真实地址 = 重定位表的值 + 不透明常量 (mod 2^64)
- 混淆性质：两函数都是控制流平坦化 —— ret=1，br=22（custom_enc）/ br=43（encode_record）
- 混淆机制：两级带键指针表（手工 GOT），已解出 47 槽 / 96 条映射 → ptr_map.json
  （53 条指向代码 = inner 内部函数，43 条指向数据；代码目标全部无导出符号）
- 样本特征：133 字节定长、无压缩魔数、熵 6.13、偏移 36–42 有 7 个零字节、
  两次不同分数共享 61% 字节（其中偏移 64–95 共 32 字节完全相同）、尾部 37 字节高熵（疑似 MAC）
- 已知明文检验：分数 9550 / 109839 在任何简单编码（u32 LE/BE、float、十进制 ASCII）里都找不到
  ⇒ 数字也经过了变换，不是"明文 ⊕ 固定 keystream"

【下一步照这个顺序做】
1. 用 ptr_map.json 把两个函数里每一处隐藏访问翻译成真实目标，生成「标注版反汇编」
2. 从 53 个内部代码目标里找出被 custom_enc 调用的，反汇编分类（memcpy/Vec 类 vs 真·算法逻辑）
3. 按平坦化结构（br 的目标关系）重建状态机转移图，逐块标注，还原算法
4. 判定密钥来源：算法里的常量表/密钥是静态常量还是运行时派生
   —— 这一条决定「能不能自造合法记录」，即这条路最终是否走得通
5. 若可行：在 phira/src/upload.rs 实现等价编码，把 record_data 的构造从 #[cfg(closed)] 里挪出来，
   song.rs 的 rated 改成真实判断 → 真机验证（务必用小号）

【必须一直记着的风险】
- 尾部 37 字节若是 MAC 且密钥来自运行时 → 静态逆向到不了终点，要尽早判定并如实告知
- 官方可能随时改格式
- 账号风险：调试期上传异常成绩可能被判定异常

【工作方式要求】
- 诚实报告：失败路径也要写进笔记（笔记里已有"定位 handler 区"的两次失败记录，别重走）
- 不确定就给选项 + 成本，别自行发挥；用户说"先不要做"就是别动代码
- 每阶段结果 commit + push 到两个仓库；逆向资料放 docs/rev/，结论写进 docs/上传成绩逆向笔记.md
- 用中文交流
```
